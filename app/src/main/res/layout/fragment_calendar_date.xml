<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
            name="viewModel"
            type="com.example.lifediary.ui.calendar.date.CalendarDateViewModel" />
    </data>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@style/ScreenContainer"
            android:paddingStart="16dp"
            android:paddingEnd="16dp">

            <ImageView
                android:id="@+id/calendarIcon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                android:src="@drawable/icon_calendar_logo" />

            <TextView
                android:id="@+id/title"
                style="@style/Text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:layout_constraintTop_toTopOf="@id/calendarIcon"
                app:layout_constraintBottom_toBottomOf="@+id/calendarIcon"
                app:layout_constraintStart_toEndOf="@id/calendarIcon"
                android:textColor="@color/app_green"
                android:textSize="24sp"
                tools:text="16.08.2021"
                android:text="@{viewModel.title}" />

            <com.example.lifediary.ui.common.WeatherForecastView
                android:id="@+id/weatherForecastView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toEndOf="@id/title"
                app:layout_constraintEnd_toEndOf="parent"
                app:booleanVisibility="@{viewModel.isWeatherForecastContainerVisible()}"
                app:dayTemperature='@{String.format("%s°C", viewModel.weatherForecastForDate.temperature.dayString)}'
                app:nightTemperature='@{String.format("%s°C", viewModel.weatherForecastForDate.temperature.nightString)}'
                app:weatherIconUrl="@{viewModel.weatherForecastIconUrl}"
                app:description="@{viewModel.weatherForecastDescription}"
                app:windSpeed='@{String.format("%.1f%s", viewModel.weatherForecastForDate.windSpeed, @string/meters_per_second)}'
                app:humidity='@{String.format("%d%s", viewModel.weatherForecastForDate.humidity, "%")}' />

            <androidx.constraintlayout.widget.ConstraintLayout
                android:id="@+id/addNotesContainer"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                android:layout_marginTop="110dp"
                app:booleanVisibility="@{!viewModel.notesVisible}"
                android:onClick="@{() -> viewModel.onAddNotesClick()}">

                <ImageView
                    android:id="@+id/addNotesButton"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    android:src="@drawable/icon_plus_green" />

                <TextView
                    android:id="@+id/addNotesTextView"
                    style="@style/Text.MainControlsHint"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:layout_constraintStart_toEndOf="@id/addNotesButton"
                    app:layout_constraintTop_toTopOf="@id/addNotesButton"
                    app:layout_constraintBottom_toBottomOf="@id/addNotesButton"
                    android:text="@string/add_note" />

            </androidx.constraintlayout.widget.ConstraintLayout>

            <androidx.constraintlayout.widget.ConstraintLayout
                android:id="@+id/notesContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:layout_constraintTop_toTopOf="parent"
                android:layout_marginTop="110dp"
                app:booleanVisibility="@{viewModel.notesVisible}">

                <com.example.lifediary.ui.common.RollOutTextPanel
                    android:id="@+id/notesView"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintEnd_toStartOf="@id/editNotesButton"
                    app:text="@{viewModel.notesText}" />

                <ImageView
                    android:id="@+id/editNotesButton"
                    android:layout_width="40dp"
                    android:layout_height="32dp"
                    app:layout_constraintTop_toTopOf="@id/notesView"
                    app:layout_constraintEnd_toStartOf="@id/deleteNotesButton"
                    android:paddingStart="8dp"
                    android:src="@drawable/icon_edit_blue"
                    android:onClick="@{() -> viewModel.onEditNotesClick()}" />

                <ImageView
                    android:id="@+id/deleteNotesButton"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    app:layout_constraintTop_toTopOf="@id/notesView"
                    app:layout_constraintEnd_toEndOf="parent"
                    android:src="@drawable/icon_delete_red"
                    android:onClick="@{() -> viewModel.onDeleteNotesClick()}" />

            </androidx.constraintlayout.widget.ConstraintLayout>

        </androidx.constraintlayout.widget.ConstraintLayout>
    </ScrollView>
</layout>