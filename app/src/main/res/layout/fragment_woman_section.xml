<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
            name="viewModel"
            type="com.example.lifediary.ui.woman_section.WomanSectionViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        style="@style/ScreenContainer">

        <TextView
            android:id="@+id/lastMenstruationTitle"
            style="@style/Text.ScreenSubtitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/menstruationDatesListButton"
            android:text="@string/last_menstruation" />

        <TextView
            android:id="@+id/lastMenstruationDates"
            style="@style/Text.MediumGray"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/lastMenstruationTitle"
            app:layout_constraintStart_toStartOf="@id/lastMenstruationTitle"
            app:layout_constraintEnd_toStartOf="@id/menstruationDatesListButton"
            tools:text="19.02.2022 - 24.02.2022"
            android:text="@{viewModel.lastMenstruationDates.toOutputFormattedString()}" />

        <ImageButton
            android:id="@+id/menstruationDatesListButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            app:layout_constraintTop_toTopOf="@id/lastMenstruationTitle"
            app:layout_constraintBottom_toBottomOf="@id/lastMenstruationDates"
            app:layout_constraintEnd_toEndOf="parent"
            android:background="@drawable/icon_list_blue"
            android:onClick="@{() -> viewModel.onShowMenstruationDatesListClick()}" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>